# Full House — Agent Guide

`backend/` — Gleam/Wisp API (port 8000). `webfrontend/` — TypeScript/Vite.

## Commands

```bash
cd backend
gleam build
gleam test   # uses wisp/simulate — no server needed
```
