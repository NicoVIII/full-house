pre-commit:
  jobs:
    - name: "skir-format"
      glob: "{lefthook.yml,skir-src/**/*.skir}"
      run: ./skir.sh format --ci
    - name: "skir-snapshot"
      glob: "{lefthook.yml,skir-src/**/*.skir}"
      run: ./skir.sh snapshot --ci
    - name: "server-format"
      glob: "{lefthook.yml,skir-src/**/*.skir,server/**/*.{gleam,toml}}"
      run: cd server && gleam format --check
    - name: "server-type-check"
      glob: "{lefthook.yml,skir-src/**/*.skir,server/**/*.{gleam,toml}}"
      run: cd server && gleam check
    - name: "server-lint"
      glob: "{lefthook.yml,skir-src/**/*.skir,server/**/*.{gleam,toml}}"
      run: cd server && gleam run -m lint
    - name: "server-test"
      glob: "{lefthook.yml,skir-src/**/*.skir,server/**/*.{gleam,toml}}"
      run: cd server && gleam test
    - name: "webclient-format"
      glob: "{lefthook.yml,skir-src/**/*.skir,client-web/**/*.{ts,tsx,json}}"
      run: cd client-web && bun run format:check
    - name: "webclient-type-check"
      glob: "{lefthook.yml,skir-src/**/*.skir,client-web/**/*.{ts,tsx,json}}"
      run: cd client-web && bun x --no-install tsc --noEmit
    - name: "webclient-lint"
      glob: "{lefthook.yml,skir-src/**/*.skir,client-web/**/*.{ts,tsx}}"
      run: cd client-web && bun run lint
    - name: "webclient-deadcode"
      glob: "{lefthook.yml,skir-src/**/*.skir,client-web/.kniprc.json,client-web/package.json,client-web/**/*.{ts,tsx}}"
      run: cd client-web && bun run check:deadcode
    - name: "webclient-test"
      glob: "{lefthook.yml,skir-src/**/*.skir,client-web/**/*.{ts,tsx,json}}"
      run: cd client-web && bun run test:run
